# Resume-Tailor v0.6 Implementation Report

## Scope

Implemented the guided conversational entry point defined in
`docs/requirements-v0.6.md`.

## Delivered

- Added `/resume-tailor` and `$resume-tailor` trigger descriptions.
- Added an eight-phase guided workflow reference.
- Required one blocking question per reply.
- Added file-based phase detection for safe resume behavior.
- Added input routing for resume text/files, job JSON, screenshots, pasted JD,
  and browser-extension exports.
- Made successful validation and `report.html` the core completion gate.
- Kept experience deepening, detailed diagrams, and Cover Letters after the
  first report.
- Updated UI metadata to start with `/resume-tailor`.
- Added automated Skill contract tests.

## Local Installation

The six `resume-tailor*` Skill directories are linked into the user's personal
agent Skill directory. Because links point to this repository, future Skill
edits take effect without copying files again.

New sessions should use this repository as the workspace and send:

```text
/resume-tailor
```

## Verification

- 41 tests passed.
- Skill directory names and frontmatter match.
- Every Skill has `agents/openai.yaml`.
- The main Skill links the guided reference.
- The guide requires Validator success and an HTML report.
- Existing report-template worktree changes remained intact.
